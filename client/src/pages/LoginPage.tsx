// Placeholder — the real login form (useLogin + useAuth) lands next.

export function LoginPage() {
  return (
    <div>
      <h1 className="text-2xl font-bold">Login</h1>
      <p className="text-muted-foreground">The login form is coming next.</p>
    </div>
  );
}
