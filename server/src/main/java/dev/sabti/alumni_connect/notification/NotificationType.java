package dev.sabti.alumni_connect.notification;

// Candidate-facing outcomes only for now; the same three events that send an email.
public enum NotificationType {
    APPLICATION_ACCEPTED,
    APPLICATION_REJECTED,
    INTERVIEW_SCHEDULED
}
