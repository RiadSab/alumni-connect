services:
  db:
    image: postgres:16
    environment:
      POSTGRES_DB: alumni_connect
      POSTGRES_USER: alumni
      POSTGRES_PASSWORD: ${DB_PASSWORD}
    ports: ["5432:5432"]
    volumes: ["pgdata:/var/lib/postgresql/data"]
volumes:
  pgdata:
